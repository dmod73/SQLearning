--En este ejemplo estamos viendo como el MAX esta seleccioando el numero mayor de la columna Age
SELECT MAX(Age) FROM Users
--En este ejemplo estamos viendo como el MIN esta seleccioando el numero Menor de la columna Age
SELECT MIN(Age) FROM Users
